// Defaults
tutorial_type  = "";    // "PATH", "CIPHER", or "QTE"
text_title     = "";
text_body      = "";
next_object    = noone; // What minigame to spawn when this closes?
tutorial_image      = -1;  // Sprite for page 2 (-1 = text-only, no page 2)
tutorial_image_body = "";  // Caption lines shown below the image

// Paging
page  = 1;
alpha = 0;

// Page 1 box dimensions (fixed)
box_width  = 400;
box_height = 250;

depth = -10000;