// CREATE

fade_mode = "fading_in";
fade_speed = 0.01; // Slower!
fade_alpha = 1;
target_room = noone;

// NEW! A timer for how long to stay black (60 frames = 1 second)
fade_timer_max = 60;
fade_timer = fade_timer_max;

// NEW: Coordinates to move the player to
target_x = 0;
target_y = 0;