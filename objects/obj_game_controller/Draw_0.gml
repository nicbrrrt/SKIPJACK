// Objective waypoint arrow (world space)
if (room == rm_combat || room == rm_battle_scramble || room == rm_menu
	|| room == rm_cipher_select || room == rm_settings) exit;

scr_draw_objective_arrow();
